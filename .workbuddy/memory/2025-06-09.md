# 2025-06-09

## OneFolio 架构解耦 — 第二步

### 目标
拆除 `lib/` ↔ `domains/` 的双向依赖

### 执行

**Phase 2A: 修复 `domains/task/processor.ts` 循环依赖**
- `import { ... } from '@/lib/task'` → `import { ... } from '@/domains/task/config'`
- 消除了 `domain → lib → domain` 的完整环

**Phase 2B: 更新 `app/api/analytics/route.ts` 导入**
- AnalyticsService + 15 个类型从 `@/lib` 改为 `@/domains/analytics`

**Phase 2C: 更新 `app/api/system-status/route.ts` 导入**
- AnalyticsService 从 `@/lib` 改为 `@/domains/analytics`

**Phase 2D: 删除 `lib/index.ts` 中的 domain re-export（lines 106-141）**
- 移除 `AnalyticsService` 转发
- 移除 `getUserByUsername` 等 user service 转发
- 移除 `getOrgByTreeid` 等 org service 转发

### 验证
- `npx tsc --noEmit`: ✅ 零错误
- `pnpm build`: ✅ `Compiled successfully`，TypeScript + webpack 均通过
- 页面数据收集阶段的 `COZE_SUPABASE_URL is not set` 是预存问题（analytics/route.ts 第 36 行模块级 `getServiceSupabaseClient()` 调用），与本次迁移无关

### 现状
- `lib/` 层不再 import 任何 `@/domains/*`
- `lib/task/` 和 `lib/auth/` 保留为向后兼容桶导出（仅 re-export domains，无循环）
- 依赖方向：`app/api/* → lib/* → @/lib/*` 和 `app/api/* → @/domains/*` 两条路并行
